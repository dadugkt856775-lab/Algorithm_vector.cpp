# Algorithm_vector.cpp
A vector is one of the most commonly used containers in the C++ Standard Template Library (STL). It is a dynamic array that can automatically increase or decrease its size when elements are added or removed. Unlike a normal array, whose size is fixed after declaration, a vector provides flexible memory management.
